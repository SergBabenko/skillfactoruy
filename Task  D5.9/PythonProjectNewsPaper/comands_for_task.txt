
1. Создать двух пользователей (с помощью метода User.objects.create_user('username')).
 user1 = User.objects.create_user('Petya')
 user2 = User.objects.create_user('Vasya')

2. Создать два объекта модели Author, связанные с пользователями.
 author1 = Author.objects.create(user=user1)
 author2 = Author.objects.create(user=user2)

3. Добавить 4 категории в модель Category.
 cat1 = Category.objects.create(name='Спорт')
 cat2 = Category.objects.create(name='Новости')
 cat3 = Category.objects.create(name='Здоровье')
 cat4 = Category.objects.create(name='Советы')

4. Добавить 2 статьи и 1 новость.
 post1 = Post.objects.create(author=Author.objects.get(id=1), post_type='Articles', title='Что то интересное про спорт и здоровье', text='многобукавкавоченьмногобукавков')
 post2 = Post.objects.create(author=Author.objects.get(id=2), post_type='Articles', title='Советы по здоровью', text='ещебольшебукавковмногобукавкавоченьмногобукавков')
 post3 = Post.objects.create(author=Author.objects.get(id=1), post_type='News', title='Срочные новости и советы, что делать', text='вашпеоченьмногобукавкавещебольшебукавковмногобукавкавоченьмногобукавков')

5. Присвоить им категории (как минимум в одной статье/новости должно быть не меньше 2 категорий).
 post1.category.add(cat1, cat3)
 post2.category.add(cat4) или post2.category.add(Category.objects.get(id=4))
 post3.category.add(cat2, cat4)

6. Создать как минимум 4 комментария к разным объектам модели Post (в каждом объекте должен быть как минимум один комментарий).
 comment1 = Comment.objects.create(post_id=1,user=user2, text='Очень интересно, ничего не понятно!)')
 comment2 = Comment.objects.create(post=Post.objects.get(id=2),user=user2, text='Спасибо, пригодилось!')
 comment3 = Comment.objects.create(post=Post.objects.get(id=1),user=user1, text='Очень познавательно!')
 comment4 = Comment.objects.create(post_id=3,user=user1, text='А хорошие новости будут?')

7. Применяя функции like() и dislike() к статьям/новостям и комментариям, скорректировать рейтинги этих объектов.
 post1.like()
 post1.dislike()
 comment2.like()
 comment2.dislike()

8. Обновить рейтинги пользователей.
 author1.update_rating()
 author2.update_rating()

9. Вывести username и рейтинг лучшего пользователя (применяя сортировку и возвращая поля первого объекта).
 best_author = Author.objects.order_by('-rating').first()
 best_author

10. Вывести дату добавления, username автора, рейтинг, заголовок и превью лучшей статьи, основываясь на лайках/дизлайках к этой статье.
 best_post = Post.objects.order_by('-rating').first()
 Post.objects.filter(id=3).values('created_at', 'author', 'rating', 'title')
 best_post.preview()

11. Вывести все комментарии (дата, пользователь, рейтинг, текст) к этой статье.
 comments = Comment.objects.filter(post=best_post)
 comments.values('created_at', 'user', 'rating', 'text')